services:
  kv-store:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 512M
        reservations:
          cpus: '1.0'
          memory: 256M